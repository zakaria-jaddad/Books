#ifndef HEADER_H
#define HEADER_H

#include <stdio.h>
#include <stdlib.h>

#define LINE_LIMIT 1337

/*
 * ft_getline: get a line from the standard input,
 * takes a limit which is buffer limit
 */
char *ft_getline(unsigned int limit);

#endif
